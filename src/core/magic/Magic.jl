# Deprecated compatibility wrapper.
#
# The implementation has been consolidated into `magic.jl` and the `magic` module.
# This file remains only to avoid breaking legacy includes.

include("magic.jl")
